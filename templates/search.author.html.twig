
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <link rel="stylesheet"  href="{{ asset('css/exo_style.css') }}"  />
</head>
<body>
<header >
    <div id="menu">
        <img src="/img/logo.png"   />
        <div>
            <ul >
                <li><a href="{{ path('accueil') }}">ACCUEIL</a></li>
                <li>LIVRES</li>
                <li>DIVERS</li>
            </ul>
            <div>
                <form action="{{ path('author_search') }}" method="get">
                    <label for="word"></label>
                    <input type="text" name="word" placeholder="Rechercher sur le site…">
                    <button>Rechercher</button>

                </form>
            </div>
        </div>
    </div>
</header>

<main>

    <h4>Votre recherche : {{ word }}</h4>
    <div class="search_result">
        <h3>RESULTAT :</h3>

            {% for author in authors %}

                <p class="titre">{{ author.name }} {{ author.firstName }}</p>
                <p>Date de naissance : {{ author.birthDate|date('Y-m-d') }}</p>
                <p>Date de décès : {{ author.deathDate|date('Y-m-d') }}</p>
                <p>Biographie : {{ author.biography }}</p>

            {% endfor %}
    </div>
    <a href="{{ path('accueil') }}">RETOUR A LA PAGE D'ACCUEIL</a>


</main>
<footer>
    <div id="menu">
        <img src="/img/logo.png"   />
        <div>
            <ul >
                <li>Lien 1</li>
                <li>Lien 2</li>
                <li>Lien 3</li>
            </ul>
        </div>

    </div>
</footer>
</body>
</html>

